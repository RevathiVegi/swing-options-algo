Swing Options Algo — Quick Start

1) Unzip Swing-Options-Algo-v1.0.0.zip into a folder on your PC.
2) Double-click START_SYSTEM.bat (Windows) OR run ./start.sh (Linux/macOS).
3) Wait for 'System started.' message.
4) Results appear in the 'out' folder; logs in the 'logs' folder.
5) To stop, double-click STOP_SYSTEM.bat or run ./stop.sh.

If anything goes wrong:
 - Restart (stop then start).
 - Send the most recent file from 'logs' to Support.

Support Contact: <your-email@example.com>
